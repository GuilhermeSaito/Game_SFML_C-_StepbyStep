#include "mainController.h"

int main(int argc, char** argv)
{
    MainController mainController(argc, argv);

    return 0;
}

/*
    Things that need to be fixed

    * The jump, in the class "Player"
    * The view in the player, in the class "Player"         <--- The view cam be in other class, or in the sef class, because of the other window.
*/